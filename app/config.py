# Configuration settings for the Conversational Bot application

class Settings:
    APP_NAME: str = "Conversational Bot for Employee Engagement"
    DEBUG: bool = True
    # Add additional configuration variables as needed

settings = Settings()
